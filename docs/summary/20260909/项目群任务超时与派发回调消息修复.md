# 项目群任务超时与派发回调消息修复（v1.7.15 / v1.7.16）

- 日期：2026-09-09
- 版本：v1.7.14 → **v1.7.15**（群回合 30 分钟预算 + 派发/回调普通气泡）→ **v1.7.16**（追加：模型 4xx 错误友好文案，见文末）
- 平台：Linux（AppImage + deb）、Windows（NSIS，wine64 打包）

## 问题一：群任务执行报「处理消息失败：The operation was aborted due to timeout」

### 现场与根因

- 日志：`.tmp/logs/debug-20260909.log`。失败的「项经理」回合从 `20:01:18.037` 到 `20:11:18.054`，间隔约 **600,017ms**，与 `OcClient.sendMessage()` 默认 `timeoutMs=600000` 精确吻合。
- 错误由 `AbortSignal.timeout(timeoutMs)` 触发（`TimeoutError`），**不是**用户点击停止（无 `group-send-stop`/`abort-fail`），**不是**先前已修复的 Undici `HeadersTimeout`（默认 300s，v1.7.11 已关为 0），也**不是** `jeff_delegate` 子调用失败（无 `delegate-fail`）。
- 普通群流水线（leader 首回合 / `@成员` worker 回合 / leader 总结）此前未显式传超时，继承默认 10 分钟；长任务（读逐字稿、产出文档）超过 10 分钟即在预算终点被掐断。
- 次要因素：日志 `20:03:06` 记录了活跃回合期间由 registry dirty 触发的 sidecar 重启（`GroupChat.doEnsureSession → restartIfRegistryDirty`）。它不是本次精确 600s 超时的直接错误类型，但会打断在途请求，本次一并防护。

### 修复

1. **统一 30 分钟回合预算**：`group.ts` 新增导出 `GROUP_TURN_TIMEOUT_MS = 30*60*1000`，`runTurn()` 显式传给 `sendMessage()`；`delegate.ts` 的 `DELEGATE_TIMEOUT_MS` 改为引用同一常量，两条任务路径不再漂移。超时后仍不自动重放 POST（任务有副作用，防重复执行）；用户主动停止的判定逻辑不变。
2. **sidecar 重启延迟落闸**：`restartIfRegistryDirty()` 遇到在途群工作（`GroupChat.hasBusyPipeline()` 或 `Delegator.activeCount>0`）时挂起 `pendingRegistryRestart`，由流水线 `onPipelineIdle` / 委派 `onIdle` 回调在空闲后触发重启；不再把长任务打断。

## 问题二：任务派发/回调显示为中间灰色小字且不完整

### 根因

- 灰色小字由 `sender_type: 'system'` 决定（`GroupWindow.tsx` 渲染分支 + `.msg-system` 样式）：
  - `jeff_delegate` 派发公告是 system 消息，且指令被 `slice(0, 120)` 截断（附录A 中「🔗 项经理 委派任务给 项小沐：…」即此）；
  - 委派失败回调也是 system 消息，错误被截 300 字；
  - 普通流水线 leader 拆解后还有一条冗余的「📋 已拆解任务，开始按序派发…」灰色公告。
- worker 汇报提示词要求 `@leader`，而需求是 `@发起任务的用户`（本产品单真人身份，即 `@我`）。

### 修复

| 消息 | 改后形态 |
|---|---|
| 委派派发 | leader **普通气泡**：`@我 已将任务派发给 @<成员>，任务要求如下：<完整指令>`，不截断；`meta.phase='dispatch'` |
| 委派成功回调 | worker **普通气泡**：`ensureCallbackMention()` 确保含 `@我`；完整 resultText；`meta.phase='result'` |
| 委派失败回调 | worker **普通气泡**：`@我 任务执行失败：<完整错误 message>`，不截断；堆栈只进调试日志；`meta.phase='failed'` |
| 流水线 worker 回调 | 落库前 `ensureCallbackMention()` 兜底补 `@我`（模型未按约定输出也不丢语义）；briefing 与派发 prompt 的汇报对象由 `@leader` 改为 `@我` |
| 「已拆解任务」公告 | 删除：派发内容已由 leader 普通消息完整承载 |

- 直连 worker 场景的 leader 自动总结不再依赖 `@leader` 提及判断（worker 回调对象已改为 `@我`），改为直连执行完成即触发。
- 前端零改动：agent 消息分支本就提供头像/名称/完整 Markdown 气泡。
- 附带修复：主进程补上 `core.bus.on('group-updated') → broadcast` 转发（此前委派写库后渲染层拿不到即时事件）；Delegator 通知携带冻结的 `{projectId, threadId}`，防切换会话后刷错线。

## 测试与验证

- `npm test`：**144 passed**（22 files；含新增/更新的 group、delegate 用例）
  - 新增：30 分钟超时预算逐回合断言、worker 未输出 `@我` 时编排器兜底、派发公告超长指令不截断、失败回调完整 message（400 字）不截断、notify 携带 threadId；
  - 更新：派发为 leader assistant 气泡断言、无「已拆解任务」灰字、直连场景总结消息数（5 条）。
- `npm run test:e2e`：**1 passed**（UI 封闭清单）。
- `npx tsc -p packages/core/tsconfig.json --noEmit` / desktop 同：通过。
- 构建：`npm run package:linux` → `Jeff-1.7.15.AppImage` + `jeff-desktop_1.7.15_amd64.deb`；`npm run package:win` → `jeff-Setup-1.7.15.exe`。
- 本机验证：`sudo dpkg -i` 覆盖安装成功（`install ok installed 1.7.15`）；冒烟脚本 9 张截图正常；「设置 → 关于」显示 **v1.7.15**、引擎 running。Wine 启动 `jeff-Setup-1.7.15.exe` 弹出安装向导（仅安装器冒烟，不等同 Windows 原生运行回归）。

## 已知限制 / 后续

- 直连 worker（用户 `@worker` 直接派活）完成后，leader 总结现在必然触发（旧逻辑仅在 worker 回复含 `@leader` 时触发；提示词改造后该信号已不可靠）。若希望直连「快问快答」不唤醒 leader，需要再引入任务类型区分。
- sidecar 重启延迟只覆盖项目群工作（流水线/委派）；私聊回合在途时重启仍会发生，后续可按需扩展。
- 结构化 mention（可点击、多真人用户身份）未引入；当前 `@我` 为单用户显示约定，文本协议即可满足。

## Git

- `git add`：core（group.ts/delegate.ts/index.ts/version.ts）、desktop（main/index.ts）、三处 package.json + package-lock.json、测试（group/delegate/e2e.group）、本总结文档；`.tmp/`、`release/` 产物均不提交。
- 提交并 push `main`，**不打 tag、不发 Release**。

---

# 追加问题：assistant 400 错误的友好文案映射（v1.7.16）

## 现象与根因

v1.7.15 验证期间（Windows 机，日志 23:04–23:09）复用项经理 session 续做任务时，回合以如下错误收场：

```json
{"name":"APIError","data":{"message":"Bad Request: {\"object\":\"error\",\"model\":\"deepseek-v4-flash\"}",
  "statusCode":400,"isRetryable":false,"metadata":{"url":"https://opencode.ai/zen/go/v1/chat/completions"}}}
```

判定：

- **不是 Jeff 编排层 bug**：同一会话、同一模型、同一思考档位（variant: high）在同轮内多次子请求全部成功，仅最后一条被上游拒绝；错误体是 opencode Zen 网关（`opencode-go` 供应商）自己返回的，`isRetryable:false`。
- 同时确认 v1.7.15 的修复都在正常工作：`send-start` 中 `timeoutMs: 1800000`（30 分钟预算生效）、项经理延用 `ses_f7a07b…`（session 复用生效）、失败后延迟重启才落闸（`flushPendingRegistryRestart`，未打断在途任务）。
- 最可能触发原因：session 延用导致**累积上下文超长**被网关以 400 拒绝；或网关对该模型的瞬时故障。无法从本机进一步定位，需 zen 侧日志。
- 真正的产品问题是**错误展示**：群里直接铺出 300 字截断的原始 JSON，用户无从处置。

## 修复（v1.7.16，PATCH）

- `OcClient` 新增导出 `friendlyAssistantError()`：按 `APIError.data.statusCode` 映射为可读中文提示，并从 `responseBody` 提取模型名：
  - 400 →「模型服务拒绝了本次请求（400）（模型 xxx）：常见原因是会话上下文超长、模型暂不可用或请求参数不被支持。可新建话题（清空上下文）后重试，或在 设置→模型供应商 更换模型。」
  - 401/403 → 检查 API Key；404 → 模型不存在/已下线；429 → 限流稍后重试；5xx → 服务暂时故障。
  - 无法识别时回退原有原始截断展示；文案不含「abort」字样（防误判「已停止生成」）。
- `sendMessage()` 的 `assistant-error` 分支优先使用友好提示；完整原始错误仍完整写入调试日志（`assistant-error` 标签），私聊/群聊/委派所有路径自动受益。
- 过程中修正一个险些引入的问题：编辑时误把该日志行的 `sessionId: input.sessionId` 简写成未定义的 `sessionId`（会抛 ReferenceError 吞掉真实错误），单测捕获后已还原。

## 测试与验证

- `tests/oc.test.ts` 新增两组用例：mock sidecar 复现 Zen 400 → 断言抛出友好提示、不含原始 JSON/abort、调试日志保留完整错误；`friendlyAssistantError` 各状态码映射与 null 回退。
- `npm test` **146 passed**；`npm run test:e2e` 通过；core/desktop tsc 通过。
- 构建 `Jeff-1.7.16.AppImage`、`jeff-desktop_1.7.16_amd64.deb`、`jeff-Setup-1.7.16.exe`；本机 dpkg 覆盖安装成功，「设置 → 关于」确认 v1.7.16、引擎 running。

## 用户侧处置建议（针对 400 本身）

- 同一群话题长期复用 session 会持续累积上下文，长项目建议适时**新建话题**清空上下文再 `@成员` 继续；
- 若反复出现 400，先到「设置 → 模型供应商」更换/核对模型（`deepseek-v4-flash` 在 Zen 网关的可用性以 zen 侧为准），必要时反馈 opencode Zen。
