{
  "name": "@jeff/core",
  "version": "1.7.4",
  "private": true,
  "type": "module",
  "description": "Jeff 核心：数据库、sidecar 管理、opencode 客户端、编排、记忆、同步",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "webdav": "^5.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
